#include <stdio.h>           // เรียกใช้ไลบรารีที่ใช้ฟังก์ชัน prntf

int main(){                  // ฟังก์ชันหลักที่โปรแกรมจะเริ่มทำงาน
    int a = 10;              //ประกาศตัวแปร a เป็นจำนวนเต็ม และกำหนดค่าเริ่มต้นเป็น 10
    printf("a =%d",a);       //แสดงข้อความพร้อมค่าของตัวแปร a
    return 0;                // ส่งค่ากลับ 0 เพื่อจบโปรแกรม

}